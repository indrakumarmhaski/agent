import { AsyncDeployResultJson, Formatter } from '../utils/types.js';
export declare class AsyncDeployResultFormatter implements Formatter<AsyncDeployResultJson> {
    private id;
    private bin;
    constructor(id: string, bin: string);
    getJson(): Promise<AsyncDeployResultJson>;
    display(): void;
}
