import BaseRandomMasking from './base-random-masking';
import { MaskingDetails } from '../../../interfaces';
export default class TimeRandomMasking extends BaseRandomMasking<string> {
    private readonly maxTime;
    private readonly minTime;
    constructor(maskingDetails: MaskingDetails);
    execute(_value: string): string;
}
