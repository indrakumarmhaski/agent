import BaseRandomMasking from './base-random-masking';
import { MaskingDetails } from '../../../interfaces';
export default class DoubleRandomMasking extends BaseRandomMasking<number> {
    private readonly maxNumber;
    private readonly minNumber;
    constructor(maskingDetails: MaskingDetails);
    execute(_value: number): number;
}
