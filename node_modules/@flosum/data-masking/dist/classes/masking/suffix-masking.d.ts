import BaseMasking from './base-masking';
import { MaskingDetails } from '../../interfaces/masking-details.interface';
export default class PrefixMasking extends BaseMasking<string> {
    private readonly suffix;
    constructor(maskingDetails: MaskingDetails);
    execute(value: string): string;
}
