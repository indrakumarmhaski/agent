import { DescribeField } from '../../interfaces/describe.interfaces';
import { MaskingDetails } from '../../interfaces/masking-details.interface';
export default abstract class BaseMasking<T> {
    protected readonly describeField: DescribeField;
    constructor(maskingDetails: MaskingDetails);
    abstract execute(value: T): T;
}
