export default abstract class BaseDateFormatter {
    protected readonly date: Date;
    constructor(date: Date | number);
    abstract toString(): string;
    protected formatDate(): {
        year: string;
        month: string;
        day: string;
        hours: string;
        minutes: string;
        seconds: string;
    };
}
