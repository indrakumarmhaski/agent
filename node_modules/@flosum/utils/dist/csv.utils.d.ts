export declare class CsvUtils {
    static stringify(records: Record<string, any>[], header?: boolean): string;
    static calculateHeaderSize(fields: string[]): number;
    static calculateRecordSize(record: Record<string, any>): number;
}
