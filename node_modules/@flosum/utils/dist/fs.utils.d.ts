/// <reference types="node" />
/// <reference types="node" />
import * as stream from 'stream';
export declare class FsUtils {
    static isExistsPath(itemPath: string): Promise<boolean>;
    static encryptCompressFile(file: string | Buffer, key: string): Promise<Buffer>;
    static decryptDecompressFile(file: Buffer, key: string): Promise<Buffer>;
    static compressEncryptPipe(stream: stream.Readable, key: string): stream.Transform;
    static decryptDecompressPipe(stream: stream.Readable, key: string): Promise<stream.Transform>;
    static getFolderSize(dirPath: string): Promise<number>;
    static getFolderFiles(dirPath: string, arrayOfFiles?: string[]): Promise<string[]>;
}
