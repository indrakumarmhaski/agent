export declare class MicroservicesUtils {
    private static createServiceRequest;
    /**
     * This method will wait service to up and running
     * NOTE: service should ping-pong lifechecking on /api/v1/ping
     * @param serviceUrl base url for service
     */
    static waitForService(serviceUrl: string): Promise<void>;
}
