export declare class SearchUtils {
    /**
     * The function only works with a sorted array.
     *
     * Parameters:
     * first:
     * The array by which the search will be carried out
     *
     * second:
     * function that will return:
     * return 0: if the item matches the condition
     * return -1: if need to continue searching on the left side of the array
     * return 1: if need to continue searching on the right side of the array
     *
     *
     * Return value:
     * null or item
     * */
    static binary<T>(array: T[], comparator: (item: T) => number): T | null;
}
