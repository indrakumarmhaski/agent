/// <reference types="node" />
/// <reference types="node" />
/// <reference types="node" />
import crypto from 'crypto';
import { Readable } from 'stream';
export declare class CryptoUtils {
    private static getCipherKey;
    static encrypt(file: string | Buffer, password: string): Buffer;
    static decrypt(file: Buffer, password: string): Buffer;
    static createEncryptPipe(password: string): crypto.Cipher;
    static createDecryptPipe(password: string, vector: Buffer): crypto.Decipher;
    static readVectorFromStream(stream: Readable, vectorSize: any): Promise<string>;
}
