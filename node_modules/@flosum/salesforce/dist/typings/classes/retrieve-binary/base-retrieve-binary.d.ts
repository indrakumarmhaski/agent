import { AxiosInstance } from 'axios';
import { RetrieveBinaryResult } from '../../interfaces/retrieve-binary.interfaces';
export default abstract class BaseRetrieveBinary {
    protected readonly objectName: string;
    protected readonly fields: string[];
    protected readonly instance: AxiosInstance;
    constructor(objectName: string, fields: string[], instance: AxiosInstance);
    abstract execute(recordsId: string[]): Promise<RetrieveBinaryResult[]>;
}
