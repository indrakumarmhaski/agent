/// <reference types="node" />
/// <reference types="node" />
import { Transform, TransformCallback } from 'stream';
export interface BulkXmlStreamParserOptions {
    binaryFields: string[];
    cast?: (value: string, field: string) => any;
}
export default class BulkXmlStreamParser extends Transform {
    private readonly binaryFields;
    private readonly cast;
    private readonly parser;
    private parsedRecord;
    private field;
    private value;
    private binaryValue;
    private binaryRemainder;
    constructor({ binaryFields, cast }: BulkXmlStreamParserOptions);
    private init;
    private handleStartElement;
    private handleText;
    private handleEndElement;
    private convertChunkBase64ToBuffer;
    private addFieldToRecord;
    _transform(chunk: string, _: BufferEncoding, callback: TransformCallback): Promise<void>;
}
