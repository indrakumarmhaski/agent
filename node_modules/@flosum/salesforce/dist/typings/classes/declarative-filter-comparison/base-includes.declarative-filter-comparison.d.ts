import BaseDeclarativeFilterComparison from './base.declarative-filter-comparison';
export default abstract class BaseIncludesDeclarativeFilterComparison extends BaseDeclarativeFilterComparison {
    protected compare(): boolean;
    private static isIncludesMultipicklists;
}
