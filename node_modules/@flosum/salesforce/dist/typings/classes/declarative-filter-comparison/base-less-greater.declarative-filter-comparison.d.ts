import BaseDeclarativeFilterComparison from './base.declarative-filter-comparison';
export default abstract class BaseLessGreaterDeclarativeFilterComparison extends BaseDeclarativeFilterComparison {
    execute(): boolean;
    protected abstract compare(recordValue: string | number, filterValue: string | number): boolean;
}
