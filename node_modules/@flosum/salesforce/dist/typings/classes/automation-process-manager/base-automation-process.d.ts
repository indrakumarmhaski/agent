import { AxiosInstance } from 'axios';
import JSZip from 'jszip';
import { RetrieveType } from '../../interfaces/metadata/retrieve-metadata.interfaces';
import { MetadataType } from '../../enums/type.metadata.enum';
export default abstract class BaseAutomationProcess {
    protected readonly instance: AxiosInstance;
    protected readonly objects: string[];
    protected readonly sourceZip: JSZip;
    protected readonly targetZip: JSZip;
    protected targetManifest: Map<MetadataType | string, Set<string>>;
    constructor(instance: AxiosInstance, objects: string[], sourceZip: JSZip, targetZip: JSZip);
    abstract disable(): Promise<void>;
    abstract getSourceManifest(): Promise<RetrieveType[]>;
    getTargetManifest(): Promise<RetrieveType[]>;
    protected query(query: string): Promise<Record<string, any>[]>;
    protected initTypeInTargetManifest(type: string): void;
    protected getDeveloperName(fileName: string): string;
}
