import { RetrieveType } from '../../interfaces';
import BaseAutomationProcess from './base-automation-process';
export default class LookupFiltersProcess extends BaseAutomationProcess {
    private setStatus;
    private addToTargetManifest;
    disable(): Promise<void>;
    getSourceManifest(): Promise<RetrieveType[]>;
}
