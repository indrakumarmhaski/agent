export declare type PKChunk = [string, string];
export declare type PKChunks = PKChunk[];
export default class PkChunking {
    private static createRanges;
    private static convertBase62ToId;
    private static convertRangesToPKChunks;
    /**
     * Method to create PK ranges based on first and last id's
     * @returns array containing array with [startId, endId]
     */
    static makeRanges(firstId: string, lastId: string, chunkSize?: number): PKChunks;
}
