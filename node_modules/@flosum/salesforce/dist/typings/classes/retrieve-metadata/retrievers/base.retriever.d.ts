import { DescribeMetadataItem, ListMetadataItemDetails, MetadataItemData, RetrieverResult, RetrieveType } from '../../../interfaces';
import { AxiosInstance } from 'axios';
export default abstract class BaseRetriever {
    protected readonly apiVersion: string;
    protected readonly axiosInstance: AxiosInstance;
    protected readonly items: ListMetadataItemDetails[];
    protected readonly describe: Record<string, DescribeMetadataItem>;
    private readonly _manifestMap;
    constructor(apiVersion: string, axiosInstance: AxiosInstance, items: ListMetadataItemDetails[], describe: Record<string, DescribeMetadataItem>);
    abstract execute(): Promise<RetrieverResult>;
    protected addManifest({ type, member }: MetadataItemData['manifest']): void;
    protected convertToRetrieveTypes(items: ListMetadataItemDetails[]): RetrieveType[];
}
