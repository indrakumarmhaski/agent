import BaseRetriever from './base.retriever';
import { RetrieverResult } from '../../../interfaces';
export default class ReadMetadataRetriever extends BaseRetriever {
    private retrieve;
    private convertResult;
    execute(): Promise<RetrieverResult>;
}
