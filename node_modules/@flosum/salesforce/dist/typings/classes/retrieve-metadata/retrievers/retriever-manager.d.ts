import { DescribeMetadataItem, ListMetadataItemDetails, SoapRetrieveMetadataResult } from '../../../interfaces';
import { AxiosInstance } from 'axios';
export default class RetrieverManager {
    private readonly _apiVersion;
    private readonly _axiosInstance;
    private readonly _describe;
    protected readonly isFullParents: boolean;
    private _items;
    private _warnings;
    private _promises;
    constructor(_apiVersion: string, _axiosInstance: AxiosInstance, _describe: Record<string, DescribeMetadataItem>, isFullParents: boolean);
    private static splitListMetadataItems;
    private countFiles;
    private launchRetrieveMetadata;
    private launchReadMetadata;
    private runReadMetadata;
    private runRetrieveMetadata;
    private handleRetrieveResults;
    private handleItem;
    execute(listMetadataItems: ListMetadataItemDetails[]): Promise<SoapRetrieveMetadataResult>;
}
