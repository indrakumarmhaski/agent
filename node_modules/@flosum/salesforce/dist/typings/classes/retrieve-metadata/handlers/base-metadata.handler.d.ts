import { DescribeMetadataItem, ContentFile, ListMetadataItem, MetadataComponent } from '../../../interfaces';
export default abstract class BaseMetadataHandler {
    protected readonly describe: Record<string, DescribeMetadataItem>;
    protected readonly item: ListMetadataItem;
    protected readonly files: Record<string, ContentFile>;
    protected get filePath(): string;
    protected get metaFilePath(): string;
    constructor(describe: Record<string, DescribeMetadataItem>, item: ListMetadataItem, files: Record<string, ContentFile>);
    abstract execute(): Promise<MetadataComponent>;
    protected parseXml(xml: string): Promise<Record<string, any>>;
}
