import BaseMetadataHandler from './base-metadata.handler';
import { ContentFile, DescribeMetadataItem, ListMetadataItem, MetadataComponent } from '../../../interfaces';
export default class ChildMetadataHandler extends BaseMetadataHandler {
    private readonly _isFullParents;
    constructor(describe: Record<string, DescribeMetadataItem>, item: ListMetadataItem, files: Record<string, ContentFile>, _isFullParents: boolean);
    execute(): Promise<MetadataComponent>;
}
