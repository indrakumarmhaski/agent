import BaseFolderListMetadata from './base.folder.list-metadata';
import { ListMetadataItem, MetadataItemData } from '../../../interfaces';
export default class DefaultFolderListMetadata extends BaseFolderListMetadata {
    protected addManifest(items: ListMetadataItem[]): MetadataItemData[];
    execute(): Promise<MetadataItemData[]>;
}
