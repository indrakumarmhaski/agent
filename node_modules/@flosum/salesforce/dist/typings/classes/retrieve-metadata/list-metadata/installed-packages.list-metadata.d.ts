import { DescribeMetadataItem, MetadataItemData } from '../../../interfaces';
import { Logger } from '../../../interfaces/logger.interfaces';
import BaseListMetadata from './base.list-metadata';
import { AxiosInstance } from 'axios';
export default class InstalledPackagesListMetadata extends BaseListMetadata {
    protected readonly describeItem: DescribeMetadataItem;
    constructor({ describeItem, ...baseOptions }: {
        apiVersion: string;
        axiosInstance: AxiosInstance;
        logger: Logger;
        describeItem: DescribeMetadataItem;
    });
    private getInstalledSubscriberPackages;
    execute(): Promise<MetadataItemData[]>;
}
