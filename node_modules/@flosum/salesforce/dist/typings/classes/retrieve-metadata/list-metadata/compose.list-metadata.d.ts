import { AxiosInstance } from 'axios';
import { ListMetadataItem, ListMetadataType } from '../../../interfaces';
import { Logger } from '../../../interfaces/logger.interfaces';
export declare type ComposeListMetadataOptions = {
    apiVersion: string;
    axiosInstance: AxiosInstance;
    logger: Logger;
    types: ListMetadataType[];
};
export default class ComposeListMetadata {
    private readonly _items;
    protected readonly apiVersion: string;
    protected readonly axiosInstance: AxiosInstance;
    protected readonly logger: Logger;
    protected readonly types: ListMetadataType[];
    constructor({ apiVersion, axiosInstance, logger, types }: ComposeListMetadataOptions);
    private listMetadataChunk;
    execute(): Promise<ListMetadataItem[]>;
}
