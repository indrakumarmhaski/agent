import { DescribeMetadataResult, ListMetadataItemDetails } from '../../../interfaces';
import { Logger } from '../../../interfaces/logger.interfaces';
import { AxiosInstance } from 'axios';
export declare type ManagerListMetadataOptions = {
    apiVersion: string;
    axiosInstance: AxiosInstance;
    describeMetadata: DescribeMetadataResult;
    logger: Logger;
};
export default class ManagerListMetadata {
    private _apiVersion;
    private _axiosInstance;
    private _describeMetadata;
    private _logger;
    private _itemsByUniqueName;
    private _describeByType;
    constructor({ apiVersion, axiosInstance, describeMetadata, logger }: ManagerListMetadataOptions);
    private runListMetadata;
    private setToItemsByUniqueName;
    private isChildType;
    execute(): Promise<ListMetadataItemDetails[]>;
}
