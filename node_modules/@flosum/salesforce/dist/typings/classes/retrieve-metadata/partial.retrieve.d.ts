import { BaseRetrieve } from '..';
import { AxiosInstance } from 'axios';
import { DeclarativeFilterLine } from '../../interfaces';
import { Logger } from '../../interfaces/logger.interfaces';
export default class PartialRetrieve extends BaseRetrieve {
    private readonly metadataTypes;
    protected readonly isFullParents: boolean;
    private readonly declarativeFilterEval;
    constructor(apiVersion: string, axiosInstance: AxiosInstance, logger: Logger, filters: DeclarativeFilterLine[] | null, customLogic: string | null, metadataTypes: string[] | null, isFullParents?: boolean);
    protected listMetadata(): Promise<void>;
}
