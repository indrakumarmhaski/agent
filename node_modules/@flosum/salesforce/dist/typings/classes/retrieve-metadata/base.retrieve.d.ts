import { AxiosInstance } from 'axios';
import { DescribeMetadataResult, ListMetadataItemDetails, SoapRetrieveMetadataResult } from '../../interfaces';
import { Logger } from '../../interfaces/logger.interfaces';
export default abstract class BaseRetrieve {
    protected readonly apiVersion: string;
    protected readonly axiosInstance: AxiosInstance;
    protected readonly logger: Logger;
    protected readonly isFullParents: boolean;
    protected describeMetadataResult: DescribeMetadataResult;
    protected listMetadataItems: ListMetadataItemDetails[];
    constructor(apiVersion: string, axiosInstance: AxiosInstance, logger: Logger, isFullParents?: boolean);
    protected describeMetadata(): Promise<void>;
    private retrieve;
    protected abstract listMetadata(): Promise<void>;
    execute(): Promise<SoapRetrieveMetadataResult>;
}
