export default class ClauseBuilder {
    private _clauses;
    get hasClauses(): boolean;
    private assertInput;
    where(clause: ClauseBuilder): ClauseBuilder;
    where(input: string, parameters?: Record<string, any>): ClauseBuilder;
    andWhere(clause: ClauseBuilder): ClauseBuilder;
    andWhere(input: string, parameters?: Record<string, any>): ClauseBuilder;
    orWhere(clause: ClauseBuilder): ClauseBuilder;
    orWhere(input: string, parameters?: Record<string, any>): ClauseBuilder;
    private buildClause;
    copy(): ClauseBuilder;
    clear(): ClauseBuilder;
    build(): string;
}
