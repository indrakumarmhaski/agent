/// <reference types="node" />
import { ListMetadataItem } from '../../../interfaces';
export declare type DeployComponent = {
    files: Record<string, Buffer>;
    manifest: Record<string, string[]>;
};
export default abstract class BaseDeployPreparer {
    protected readonly manifest: Record<string, string[]>;
    protected addToManifest(item: ListMetadataItem): void;
    abstract execute(): Promise<DeployComponent>;
}
