import { MetadataComponent } from '../../../interfaces';
import BaseDeployPreparer, { DeployComponent } from './base.deploy-preparer';
export interface DefaultDeployPreparerOptions {
    main?: MetadataComponent;
}
export default class DefaultDeployPreparer extends BaseDeployPreparer {
    private readonly _component;
    constructor(_component: DefaultDeployPreparerOptions);
    execute(): Promise<DeployComponent>;
}
