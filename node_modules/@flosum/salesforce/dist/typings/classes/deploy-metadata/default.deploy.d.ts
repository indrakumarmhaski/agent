import BaseDeploy from './base.deploy';
import { CheckDeployMetadataStatusResult, CommandDeployMetadataSoapDeployOptions } from '../../interfaces/metadata/deploy-metadata.interfaces';
import { Logger } from '../../interfaces/logger.interfaces';
import { AxiosInstance } from 'axios';
import { MetadataType } from '../../enums';
import { MetadataItem } from '../../interfaces';
export default class DefaultDeploy extends BaseDeploy {
    private readonly _items;
    private readonly _manifest;
    constructor(apiVersion: string, axiosInstance: AxiosInstance, logger: Logger, _items: Record<MetadataType, MetadataItem>, deployOptions?: CommandDeployMetadataSoapDeployOptions);
    private prepareDeployOptions;
    private addToManifest;
    private getPackageXML;
    execute(): Promise<CheckDeployMetadataStatusResult>;
}
