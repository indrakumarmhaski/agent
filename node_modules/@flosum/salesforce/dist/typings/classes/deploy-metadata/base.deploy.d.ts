import { AxiosInstance } from 'axios';
import { Logger } from '../../interfaces/logger.interfaces';
import { CheckDeployMetadataStatusResult, CommandDeployMetadataSoapDeployOptions } from '../../interfaces/metadata/deploy-metadata.interfaces';
export default abstract class BaseDeploy {
    protected readonly apiVersion: string;
    protected readonly axiosInstance: AxiosInstance;
    protected readonly logger: Logger;
    protected file: string;
    protected deployOptions?: CommandDeployMetadataSoapDeployOptions;
    constructor(apiVersion: string, axiosInstance: AxiosInstance, logger: Logger);
    protected deploy(): Promise<CheckDeployMetadataStatusResult>;
    abstract execute(): Promise<CheckDeployMetadataStatusResult>;
}
