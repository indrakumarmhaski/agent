import { FieldType } from '../enums/field-type.enum';
import { DeclarativeFilterLine } from '../interfaces/declarative-filter.interfaces';
import { DeclarativeFilterOptions } from '../enums/declarative-filter.enums';
export default class DeclarativeFilterEval {
    private readonly declarativeFilterLines;
    private readonly fieldsType;
    private readonly logic;
    constructor(declarativeFilterLines: DeclarativeFilterLine[] | null, declarativeFilterCustomLogic: string | null, fieldsType: {
        [fieldName: string]: FieldType;
    });
    isMeetFilterLine(options: {
        type: FieldType;
        option: DeclarativeFilterOptions;
        filterValue: string;
        recordValue: string;
    }): boolean;
    isMeetFilterCriteria(record: {
        [fieldName: string]: string;
    }): boolean;
}
