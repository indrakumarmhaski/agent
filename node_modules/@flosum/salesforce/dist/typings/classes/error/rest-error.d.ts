import SalesforceError from '../salesforce-error.class';
import { RestErrorDetails } from '../../interfaces/rest.interfaces';
export default class RestError {
    private static parseError;
    private static parseArray;
    private static parseAxiosError;
    static parse(error: Record<string, any>): SalesforceError;
    static parse(errors: Record<string, any>[]): SalesforceError;
    static parse(error: RestErrorDetails): SalesforceError;
    static parse(errors: RestErrorDetails[]): SalesforceError;
}
