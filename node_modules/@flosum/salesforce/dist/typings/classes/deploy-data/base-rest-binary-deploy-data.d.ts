import BaseRestDeployData from './base-rest-deploy-data';
import { BinaryContent } from '../../interfaces/deploy.interfaces';
import FormData from 'form-data';
export default abstract class BaseRestBinaryDeployData extends BaseRestDeployData {
    protected abstract appendBinaries(form: FormData, binaries: BinaryContent[]): Promise<void>;
}
