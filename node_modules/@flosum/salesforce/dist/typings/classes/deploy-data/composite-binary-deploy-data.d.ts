import BaseRestBinaryDeployData from './base-rest-binary-deploy-data';
import FormData from 'form-data';
import { DeployDataResult, CompositeBinaryData, BinaryContent } from '../../interfaces/deploy.interfaces';
export default class CompositeBinaryDeployData extends BaseRestBinaryDeployData {
    execute(compositeBinaryData: CompositeBinaryData): Promise<DeployDataResult[]>;
    protected appendBinaries(form: FormData, binaries: BinaryContent[]): Promise<void>;
}
