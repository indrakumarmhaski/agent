import BaseDeployData from './base-deploy-data';
import { DeployRequest } from '../../interfaces/deploy.interfaces';
export default abstract class BaseRestDeployData extends BaseDeployData {
    constructor({ instance, objectName, externalIdField, allOrNone, api }: DeployRequest);
    close(): Promise<void>;
}
