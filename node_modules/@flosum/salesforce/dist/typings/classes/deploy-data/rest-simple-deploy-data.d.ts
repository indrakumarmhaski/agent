import BaseRestDeployData from './base-rest-deploy-data';
import { CompositeData, DeployDataResult } from '../../interfaces/deploy.interfaces';
export default class RestSimpleDeployData extends BaseRestDeployData {
    execute(payload: CompositeData): Promise<DeployDataResult[]>;
    close(): Promise<void>;
}
