import BaseRestBinaryDeployData from './base-rest-binary-deploy-data';
import FormData from 'form-data';
import { RestBinaryData, DeployDataResult, BinaryContent } from '../../interfaces/deploy.interfaces';
export default class RestSobjectBinaryDeployData extends BaseRestBinaryDeployData {
    execute(payload: RestBinaryData): Promise<DeployDataResult[]>;
    protected appendBinaries(form: FormData, binaries: BinaryContent[]): Promise<void>;
}
