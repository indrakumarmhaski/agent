import { RetrieveRequestStandardRest, RetrieveResult } from '../../interfaces/retrieve.interfaces';
import BaseSfRetrieveData from './base-retrieve-data';
export default class StandardRestRetrieveData extends BaseSfRetrieveData<RetrieveResult> {
    private readonly maxRecords;
    private readonly api;
    private readonly isQueryAll;
    private queryLocator;
    private get query();
    private get requestConfig();
    constructor({ api, queryBuilder, instance, maxRecords, isQueryAll, logger }: RetrieveRequestStandardRest);
    execute(): Promise<RetrieveResult>;
}
