import { AxiosInstance } from 'axios';
import QueryBuilder from '../query-builder';
import { Logger } from '../../interfaces/logger.interfaces';
export default abstract class BaseRetrieveData<T> {
    protected readonly instance: AxiosInstance;
    protected readonly logger?: Logger;
    protected readonly queryBuilder: QueryBuilder;
    protected isDone: boolean;
    protected numberRetrievedRecords: number;
    getIsDone(): boolean;
    protected constructor(instance: AxiosInstance, queryBuilder: QueryBuilder, logger?: Logger);
    abstract execute(): Promise<T>;
    protected isExecutable(): void;
}
