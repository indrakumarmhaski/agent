import { RetrieveRequestComposite, RetrieveResult } from '../../interfaces/retrieve.interfaces';
import BaseSfRetrieveData from './base-retrieve-data';
export default class CompositeRetrieveData extends BaseSfRetrieveData<RetrieveResult> {
    private readonly api;
    private readonly isQueryAll;
    private readonly isTooling;
    private readonly maxRecords;
    private queryLocator;
    private get query();
    constructor({ api, queryBuilder, instance, isQueryAll, isTooling, maxRecords, logger }: RetrieveRequestComposite);
    private assertCompositeResponse;
    private retrieve;
    private retrieveMore;
    execute(): Promise<RetrieveResult>;
}
