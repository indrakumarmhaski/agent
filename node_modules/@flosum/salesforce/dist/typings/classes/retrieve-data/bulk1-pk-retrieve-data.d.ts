/// <reference types="node" />
import BaseRetrieveData from './base-retrieve-data';
import { RetrieveRequestBulk1PK } from '../../interfaces/retrieve.interfaces';
import stream from 'stream';
export default class Bulk1PkRetrieveData extends BaseRetrieveData<stream.Readable | null> {
    private readonly objectName;
    private readonly api;
    private readonly pollInterval;
    private readonly contentType;
    private readonly chunkSize;
    private jobId;
    private baseBatchId;
    private currentBatchId;
    private batchesDetail;
    private batchResultIds;
    private _jobOperation;
    private lastUpdateBatchesDetail;
    private get requestConfig();
    constructor({ instance, api, pollInterval, queryBuilder, objectName, contentType, chunkSize, jobOperation, logger, }: RetrieveRequestBulk1PK);
    execute(): Promise<stream.Readable | null>;
    closeJob(): Promise<void>;
    private updateCurrentBatchId;
    private updateBatchesDetail;
    private updateBatchResultIds;
    private init;
    private createBatch;
    private getAllBatchesDetail;
    private getBatchResult;
    private waitCompletionBaseBatch;
    private createJob;
}
