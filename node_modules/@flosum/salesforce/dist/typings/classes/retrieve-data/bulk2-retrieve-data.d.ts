/// <reference types="node" />
import { RetrieveRequestBulk2 } from '../../interfaces/retrieve.interfaces';
import BaseRetrieveData from './base-retrieve-data';
import * as stream from 'stream';
export default class Bulk2RetrieveData extends BaseRetrieveData<stream.Readable | null> {
    private readonly pollInterval;
    private readonly maxRecords;
    private readonly api;
    private jobId;
    private queryLocator;
    private get query();
    constructor({ instance, queryBuilder, pollInterval, maxRecords, api, logger }: RetrieveRequestBulk2);
    private get requestResultConfig();
    execute(): Promise<stream.Readable | null>;
    abortedJob(): Promise<void>;
    private waitToCompleteJob;
}
