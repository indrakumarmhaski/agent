/// <reference types="node" />
import BaseRetrieveData from './base-retrieve-data';
import { RetrieveRequestBulk1 } from '../../interfaces/retrieve.interfaces';
import * as stream from 'stream';
export default class Bulk1RetrieveData extends BaseRetrieveData<stream.Readable | null> {
    private readonly pollInterval;
    private readonly contentType;
    protected readonly api: string;
    protected readonly objectName: string;
    private numberRecordsProcessed;
    private batchResultIds;
    private _jobOperation;
    protected jobId: string;
    protected batchId: string;
    private get requestConfig();
    protected get query(): string;
    constructor({ instance, api, pollInterval, queryBuilder, objectName, contentType, jobOperation, logger, }: RetrieveRequestBulk1);
    execute(): Promise<stream.Readable | null>;
    closeJob(): Promise<void>;
    private getResultIds;
    protected createBatch(): Promise<void>;
    private waitCompletionBaseBatch;
    protected createJob(): Promise<void>;
}
