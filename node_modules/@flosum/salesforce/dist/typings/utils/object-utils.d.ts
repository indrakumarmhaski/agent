export default class ObjectUtils {
    static convertToArrays<T extends any>(object: T, keys: string[]): void;
    static pushToArrayByKey<T extends Object>(object: T, key: string, value: any): void;
}
