import { RetrieveType } from '../interfaces';
export default class PackageXmlUtils {
    private static getPackageBase;
    private static buildPackageXml;
    static generateXml(manifest: RetrieveType[]): string;
}
