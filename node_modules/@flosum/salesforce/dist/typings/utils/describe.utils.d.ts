import { DescribeMetadataItem } from '../interfaces';
export default class DescribeUtils {
    static isRequiredField(defaultedOnCreate: boolean, nillable: boolean): boolean;
    static calculateFilesCount(describe: DescribeMetadataItem): number;
}
