import { DescribeObject } from '../interfaces/describe.interfaces';
export default class BulkUtils {
    static isSupport({ keyPrefix, name, associateEntityType }: DescribeObject): boolean;
    static isSupportPk({ name, custom }: DescribeObject): boolean;
}
