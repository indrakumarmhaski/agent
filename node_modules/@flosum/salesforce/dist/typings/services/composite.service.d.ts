import { AxiosInstance, AxiosRequestConfig } from 'axios';
import { CompositeSubRequest, CompositeResponse } from '../interfaces/composite.interfaces';
export default class CompositeService {
    static composite(instance: AxiosInstance, config: AxiosRequestConfig, requests: CompositeSubRequest[], allOrNone: boolean, api: string, isTooling?: boolean): Promise<CompositeResponse[]>;
    static subRequestDescribeObject(objectName: string, referenceId: string, api: string): CompositeSubRequest;
    static subRequestQuery(referenceId: string, query: string, api: string, isTooling?: boolean, isQueryAll?: boolean, headers?: Record<string, any>): CompositeSubRequest;
    static getReferenceId(): string;
}
