import { CheckRetrieveMetadataStatusResult, RetrieveType } from '../../interfaces';
import { AxiosInstance } from 'axios';
export default class RetrieveSoapCompose {
    private readonly _apiVersion;
    private readonly _axiosInstance;
    private readonly _types;
    private readonly _checkStatusInterval;
    private _jobId;
    constructor(_apiVersion: string, _axiosInstance: AxiosInstance, _types: RetrieveType[], _checkStatusInterval?: number);
    private launchRetrieve;
    private checkRetrieveStatus;
    execute(): Promise<CheckRetrieveMetadataStatusResult>;
}
