import { AxiosInstance } from 'axios';
import { BaseSoapCommandOptions } from '../../interfaces/metadata/base.interfaces';
import { SoapActionType } from '../../enums/soap.enums';
export default abstract class BaseSoapCommand<T, B> {
    protected readonly apiVersion: string;
    protected abstract readonly actionType: SoapActionType;
    protected get xmlBody(): string;
    protected abstract get body(): B;
    constructor({ apiVersion }: BaseSoapCommandOptions);
    request(axiosInstance: AxiosInstance): Promise<T>;
    protected abstract prepareResults(result: unknown): T;
}
