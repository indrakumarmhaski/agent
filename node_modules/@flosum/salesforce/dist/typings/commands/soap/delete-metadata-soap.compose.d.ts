import { AxiosInstance } from 'axios';
import { MetadataType } from '../../enums';
import { DeleteMetadataResult, DeleteType } from '../../interfaces/metadata/delete-metadata.interfaces';
export default class DeleteMetadataSoapCompose {
    private readonly _apiVersion;
    private readonly _axiosInstance;
    private _types;
    private readonly _items;
    constructor(_apiVersion: string, _axiosInstance: AxiosInstance, _types: DeleteType[]);
    private deleteMetadata;
    private getChunks;
    execute(): Promise<Record<MetadataType, DeleteMetadataResult[]>>;
}
