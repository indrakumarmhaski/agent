import { AxiosInstance } from 'axios';
import { CheckDeployMetadataStatusResult, CommandDeployMetadataSoapDeployOptions } from '../../interfaces/metadata/deploy-metadata.interfaces';
export default class DeploySoapCompose {
    private readonly _axiosInstance;
    private readonly _file;
    private readonly _deployOptions?;
    private _jobId;
    constructor(_axiosInstance: AxiosInstance, _file: string, _deployOptions?: CommandDeployMetadataSoapDeployOptions);
    private checkDeployStatus;
    private deploy;
    private getDeployErrorMessages;
    execute(): Promise<CheckDeployMetadataStatusResult>;
}
