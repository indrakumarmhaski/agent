import { ListMetadataCommandResult, ListMetadataType } from '../../interfaces';
import { Logger } from '../../interfaces/logger.interfaces';
import { AxiosInstance } from 'axios';
export default class ListMetadataSoapCompose {
    private readonly _apiVersion;
    private readonly _axiosInstance;
    private readonly _types;
    private readonly _logger;
    private readonly _items;
    constructor(_apiVersion: string, _axiosInstance: AxiosInstance, _types: ListMetadataType[], _logger: Logger);
    private listMetadata;
    private addFolderTypes;
    execute(): Promise<ListMetadataCommandResult>;
}
