import { ReadMetadataResult } from '../../interfaces/metadata/read-metadata.interfaces';
import { AxiosInstance } from 'axios';
import { MetadataType } from '../../enums';
import { RetrieveType } from '../../interfaces';
export default class ReadMetadataSoapCompose {
    private readonly _apiVersion;
    private readonly _axiosInstance;
    private _types;
    private readonly _items;
    constructor(_apiVersion: string, _axiosInstance: AxiosInstance, _types: RetrieveType[]);
    private readMetadata;
    private getChunks;
    execute(): Promise<Record<MetadataType, ReadMetadataResult[]>>;
}
