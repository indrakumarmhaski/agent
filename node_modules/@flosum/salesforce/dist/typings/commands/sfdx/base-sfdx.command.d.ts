import { BaseSfdxCommandOptions } from '../../interfaces/metadata/base.interfaces';
export default abstract class BaseSfdxCommand<T> {
    private readonly _cwd;
    private readonly _arguments;
    protected abstract sfdxCommand: string;
    protected constructor({ cwd }: BaseSfdxCommandOptions);
    setArgument(key: string, argument: string): void;
    run(): Promise<T>;
}
